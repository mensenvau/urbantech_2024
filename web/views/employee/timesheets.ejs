<div class="mb-3">
    <h4>Timesheets History</h4>

    <div class="row">
        <div class="card">
            <div class="card-body px-1">
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th>Date Worked</th>
                                <th>Hours Worked</th>
                                <th>Start Time</th>
                                <th>End Time</th>
                                <th>Create Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for(var i=0; i < arr.length; i++) { %>
                            <tr>
                                <td scope="row"><%= arr[i].id %></td>
                                <td><%= arr[i].date_worked %></td>
                                <td><%= arr[i].hours_worked %></td>
                                <td><%= arr[i].start_time %></td>
                                <td><%= arr[i].end_time %></td>
                                <td><%= arr[i].created_dt %></td>
                            </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <nav>
                    <ul class="pagination pagination-sm">
                        <% for(var i = 0; i < count; i++) { %>
                        <li class="page-item">
                            <a class="page-link <%= i == curr?'active':'' %>" href="?page=<%= i %>"><%= i + 1 %></a>
                        </li>
                        <% } %>
                    </ul>
                </nav>
            </div>
        </div>
    </div>
</div>
